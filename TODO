- watch rails how to video
- watch backbone and JS vids

- build basic app:

Signup and Auth
Companies
Jobs
Candidates and CV's


Tech?

Postgres for DB (users, companies, jobs, candidates)
Store CV's on Disk - have a parser to turn them into a JSON object to store somewhere searchable ? ES or MongoDB
Async upload for CV's (queue with resque)

