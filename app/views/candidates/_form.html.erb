<%= form_for(@candidate) do |f| %>
  <% if @candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
      <% @candidate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field"> <%= f.text_field :name, { class: "form-control", placeholder: 'Name' } %> </div>

  <br/>

  <div class="field"> <%= f.text_field :email, { class: "form-control", placeholder: 'Email' } %> </div>

  <br/>

  <div class="field"> <%= f.text_field :contact_number, { class: "form-control", placeholder: 'Contact Number' } %> </div>

  <br/>

  <div class="field">  <%= f.file_field :cv, { class: "form-control" } %> </div>

  <br/>

  <div class="field"> <%= f.number_field :years_of_experience, { class: "form-control", placeholder: 'Years of Experience' } %> </div>

  <br/>

  <div class="field"> <%= f.text_field :availability, { class: "form-control", placeholder: 'Availability' } %> </div>

  <br/>

  <div class="field"> <%= f.text_area :cover_letter, { class: "form-control", placeholder: 'Cover letter' } %> </div>

  <br/>

  <div class="field"> <%= f.text_area :recruiter_notes, { class: "form-control", placeholder: 'Notes' } %> </div>

  <br/>

  <div class="field">
    <%= f.label :direct %><br>
    <%= f.check_box :direct, { class: "form-control" } %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-lg btn-primary btn-block" %>
  </div>
<% end %>
